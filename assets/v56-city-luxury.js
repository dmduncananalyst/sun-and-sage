(()=>{const reveals=[...document.querySelectorAll('.cityReveal')];if('IntersectionObserver'in window){const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('cityInView');observer.unobserve(entry.target)}}),{threshold:.12});reveals.forEach(el=>observer.observe(el))}else reveals.forEach(el=>el.classList.add('cityInView'));const media=document.querySelector('[data-city-parallax] img');if(media&&!matchMedia('(prefers-reduced-motion: reduce)').matches){let queued=false;const update=()=>{const y=Math.min(38,scrollY*.055);media.style.setProperty('--city-parallax',`${y}px`);queued=false};addEventListener('scroll',()=>{if(!queued){requestAnimationFrame(update);queued=true}},{passive:true});update()}document.querySelectorAll('.cityFaq details').forEach(detail=>detail.addEventListener('toggle',()=>{if(detail.open)document.querySelectorAll('.cityFaq details[open]').forEach(other=>{if(other!==detail)other.open=false})}))})();
